services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - db-data:/app/data
    environment:
      DATABASE_URL: "sqlite+aiosqlite:///data/osmosis.db"

volumes:
  db-data:
